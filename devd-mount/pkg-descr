devd-mount(8) is a devd(8) based automounter for FreeBSD.  It supports
most popular filesystems such as NTFS, FAT, exFAT, ISO9660, UDF, and
FreeBSD's native UFS.  The devd script mounts recognized filesystems
and invokes a specified command with the mount point as an argument.
