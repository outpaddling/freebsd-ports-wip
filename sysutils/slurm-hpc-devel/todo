
Make sure job counter isn't reset when deinstalling!

Remaining two patch files appear to be unnecessary

Check %a in -o and -e.  Returns 65534 in some cases.

Test on old hardware with hwloc > 1.3.1.  Remove patch from xcpuinfo.c
if hwloc_get_nbobjs_by_type() is not returning 0.

Test cgroups plugin.  Cgroups are very Linux-specific.

Test task/affinity plugin.  Uses hwloc and cpusets.
