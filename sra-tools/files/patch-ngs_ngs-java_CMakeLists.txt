--- ngs/ngs-java/CMakeLists.txt.orig	2023-02-02 00:52:01 UTC
+++ ngs/ngs-java/CMakeLists.txt
@@ -142,6 +142,8 @@ if ( Java_FOUND )
 
         if( ${OS} STREQUAL "windows" )
             set( NGS_JAVADOC_DIR ${TARGDIR} )
+        elseif( ${OS} STREQUAL "freebsd" )
+            set( NGS_JAVADOC_DIR ${TARGDIR} )
         else()
             set( NGS_JAVADOC_DIR ${TARGDIR}/obj )
         endif()
