Cython BLIS provides the BLIS linear algebra routines as a self-contained
Python C-extension.

Currently, we only support single-threaded execution, as this is actually best
for our workloads (machine learning inference).

WWW: https://github.com/explosion/cython-blis
