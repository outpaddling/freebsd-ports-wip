--- CMakeLists.txt.orig	2025-07-27 21:00:04 UTC
+++ CMakeLists.txt
@@ -189,10 +189,10 @@ set(SHASUM ${CMAKE_CURRENT_SOURCE_DIR}/scripts/check_s
 set(SHASUM ${CMAKE_CURRENT_SOURCE_DIR}/scripts/check_shasum.sh)
 
 
-find_package(Jemalloc)
-if(JEMALLOC_FOUND)
-  include_directories(SYSTEM ${JEMALLOC_INCLUDE_DIRS})
-endif()
+# find_package(Jemalloc)
+# if(JEMALLOC_FOUND)
+  # include_directories(SYSTEM ${JEMALLOC_INCLUDE_DIRS})
+# endif()
 
 if(DO_QUIET_MAKE)
   set(QUIET_MAKE "--silent")
@@ -383,9 +383,11 @@ if(TBB_RECONFIGURE)
 
     message("TBB_INSTALL_DIR = ${TBB_INSTALL_DIR}")
   
+    # FreeBSD port rolls tbbmalloc_proxy into tbbmalloc
     find_package(TBB 2021.4 
       HINTS ${TBB_ROOT_SEARCH}
-      COMPONENTS tbb tbbmalloc tbbmalloc_proxy
+      # COMPONENTS tbb tbbmalloc tbbmalloc_proxy
+      COMPONENTS tbb tbbmalloc
     )
     
     set(TBB_LIBRARIES 
