Make netcdf-ftn a slave port

Report hdf5 issues with FreeBSD ports

Packages failing to build:
    InsightToolkit, afni, x10, nifticlib, voro++, praat

fftw-openmpi on i386
    libtool bug
    port/155546

xfce4-mount-plugin uses non-existent "eject" command

grace port needs upgrade to netcdf4

