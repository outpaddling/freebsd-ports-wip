--- src/parallel/decompose/scotch/Make/options.orig	2026-08-30 17:02:10 UTC
+++ src/parallel/decompose/scotch/Make/options
@@ -9,10 +9,12 @@ EXE_INC = \
     -I$(or $(SCOTCH_INCLUDE_DIR),/usr/include/scotch) \
     -I../decompositionMethods/lnInclude
 
+# FIXME: We should not need -L/usr/local/lib
 LIB_LIBS = \
     -L$(SCOTCH_ARCH_PATH)/lib \
     $(if $(SCOTCH_LIB_DIR),-L$(SCOTCH_LIB_DIR)) \
     -L$(FOAM_EXT_LIBBIN) \
+    -L/usr/local/lib \
     -lscotch \
     -lscotcherrexit \
     -lrt
