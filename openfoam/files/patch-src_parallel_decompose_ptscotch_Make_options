--- src/parallel/decompose/ptscotch/Make/options.orig	2026-08-25 11:46:55 UTC
+++ src/parallel/decompose/ptscotch/Make/options
@@ -11,10 +11,12 @@ EXE_INC = \
     -I$(or $(PTSCOTCH_INCLUDE_DIR),/usr/include/scotch) \
     -I../decompositionMethods/lnInclude
 
+# FIXME: We should not need -L/usr/local/lib here
 LIB_LIBS = \
     -L$(SCOTCH_ARCH_PATH)/lib \
     $(if $(PTSCOTCH_LIB_DIR),-L$(PTSCOTCH_LIB_DIR)) \
     -L$(FOAM_EXT_LIBBIN)/$(FOAM_MPI) \
+    -L/usr/local/lib \
     -lptscotch \
     -lptscotcherrexit \
     -lscotch \
