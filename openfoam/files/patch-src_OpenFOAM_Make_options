--- src/OpenFOAM/Make/options.orig	2026-08-24 20:34:35 UTC
+++ src/OpenFOAM/Make/options
@@ -2,6 +2,6 @@ LIB_LIBS = \
     -I$(OBJECTS_DIR)
 
 LIB_LIBS = \
-    $(FOAM_LIBBIN)/libOSspecific.o \
+    -L$(FOAM_LIBBIN) -lOSspecific \
     -L$(FOAM_LIBBIN)/dummy -lPstream \
     -lz
