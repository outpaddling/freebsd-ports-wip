--- /usr/wip/science/openfoam/work/OpenFOAM-3.0.0/wmake/MakefileFiles.orig	2015-11-03 04:30:23.000000000 -0600
+++ /usr/wip/science/openfoam/work/OpenFOAM-3.0.0/wmake/MakefileFiles	2015-11-13 14:55:25.000000000 -0600
@@ -55,10 +55,10 @@
 all : $(OPTIONS) $(SFILES) $(VARS)
 
 $(OPTIONS) : $(MAKE_DIR)/options
-	@$(CPP) $(GFLAGS) $(MAKE_DIR)/options | sed -e 's@   *@ @g' > $(OPTIONS)
+	@$(CPP) $(GFLAGS) $(MAKE_DIR)/options | gsed -e 's@   *@ @g' > $(OPTIONS)
 
 $(SFILES): $(MAKE_DIR)/files
-	@$(CPP) $(GFLAGS) $(MAKE_DIR)/files | sed -e 's@   *@ @g' > $(FILES)
+	@$(CPP) $(GFLAGS) $(MAKE_DIR)/files | gsed -e 's@   *@ @g' > $(FILES)
 	# Find all macro definitions in the files
 	@grep "=" $(FILES) > $(VARS)
 	echo "SOURCE = " > $(SFILES)
@@ -68,13 +68,13 @@
 	# Add a newline to files to ensure the last line is followed by a newline
 	@echo "" >> $(SFILES)
 	# Remove commented lines, blank lines, and trailing blanks from files
-	@sed -i \
+	@gsed -i \
 	    -e '/^#/ d'         \
 	    -e '/^[ \t]*$$/ d'   \
 	    -e 's,[ \t]*$$,,'    \
 	     $(SFILES)
 	# Add backslashes
-	@sed -i \
+	@gsed -i \
 	    -e 's,$$, \\,'    \
 	    -e '$$s,\\,,'    \
 	    $(SFILES)
