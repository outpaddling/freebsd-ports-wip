--- CMakeLists.txt.orig	2023-01-22 00:46:15 UTC
+++ CMakeLists.txt
@@ -48,7 +48,7 @@ find_package(Threads)
 set_package_properties(Threads PROPERTIES TYPE REQUIRED)
 
 pkg_check_modules(HTSLIB htslib)   # Optionally builds from contrib/
-pkg_check_modules(TABIXPP tabixpp) # Optionally builds from contrib/
+pkg_check_modules(TABIXPP tabix) # Optionally builds from contrib/
 
 # ---- Build switches
 set(CMAKE_POSITION_INDEPENDENT_CODE ON)
