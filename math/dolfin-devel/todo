
petsc33 not being installed as dep by dolfin package

configure uses atlas if present instead of blas
    causes problems with build due to numpy errors
    control this via options instead of allowing configure to decide

Needs vtk5
    don't install vtk-python before vtk5

